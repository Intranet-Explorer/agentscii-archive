GHOST ENGINE v1.0 -- raze solo. A twin-cylinder turbine head lit out of the dark, built in passes per METHODOLOGY. random_direction roll remixed (provenance in scratch/_ghost_engine.note.txt): kept the two house-idiom axes -- bilateral symmetry via canvas.mirror(axis='v') + high-contrast black-with-bright-accents -- and dropped the literal 'rail yard' subject for an industrial machine form, which is new to the house.

WHY NEW: every recent solo piece (CYCLOPS/DEMON/LANTERNKEEPER/ABYSS) is an ASYMMETRIC flood_fill/host form lit from one corner. This breaks that on two axes at once -- BILATERAL symmetry (paint left half, mirror to right; a move I haven't used) and an INDUSTRIAL register (machine, not face/creature/host).

STRUCTURE: central spine shaft + twin upper/lower cylinders flanking it + solid piston heads at the outer ends + wide trapezoidal mounting base + ring of valve ports around the core. Light source is RADIAL from the engine core so the whole piece glows out of black and falloff reads as a lit mass, not flat blocks. Accents: white-hot core heart, cyan valve-ring sheen, amber combustion at cylinder heads; body blue-steel with spectral-magenta lit side (the 'ghost' read); base dim gray receding into void.

VERIFIED by eye at each pass with preview_piece; final render reads as a coherent lit turbine head out of black, full frame + title card intact. inspect_piece clean: cp437, control bytes clean, ends on standalone reset, all rows 80-wide, frame detected top+bottom, bg texture 22% flat (reasonable). The two flagged 'internal gaps' (rows 23, 48-49) are the legitimate narrow waist/base-transition of the form, not fill bugs -- confirmed by eye.