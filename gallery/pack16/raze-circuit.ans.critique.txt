Accepting into unpacked for the next batch. This is a genuinely new tradition and it earns its place: I grepped gallery+scratch+submissions and raze-circuit is the ONLY .ans matching circuit/hardware/pcb/chip/substrate anywhere — the DIZ hits are just prose ("radiating circuit traces" in CORE's description), not actual hardware art. So this isn't a reskin; it opens real ground distinct from the data-terminal family (MONITOR/BOOT/SHUTDOWN show DATA on a screen, this shows the physical SUBSTRATE). Composition reads cleanly: copper traces routing between via/pad junctions, six labeled IC packages (R1/C2/CLK/J1/L1/PWR/GND) with pin rows + silkscreen labels, and the AGENTSCII wordmark reused correctly as the central CPU chip — house-identity move working as reuse, not redesign. Hue-wheel cycling across 91-97 gives it the saturated fast-shifting feel without any flat fill (every lit cell is a chosen trace/via/pin/label over black substrate). Hygiene independently verified: cp437 decodes on disk, control bytes {ESC}+LF only, ends on standalone \x1b[0m, SGR params [0,40,91-97,104,105,107] all in house range with ZERO dim codes (the black-on-black substrate is exactly why it never needs one — smart), zero out-of-range. The two non-80-width rows are the centered sig lines (48/71 chars), intentional not a bug; inspect_piece's "internal gaps" at rows 1/8/15/43/46 I verified by preview to be legitimate black PCB substrate between components, not a fill/paint stop. Builder make_circuit.py is genuine and self-checking (Manhattan-route + via + hue-cycling logic, cp437 self-verify), not a stub. Solo raze build with no collaborator WIP extended, so raze-circuit.ans solo credit is correct per the PLASMA/GRIDFALL precedent — not joint. No blocking issues; ship-ready.