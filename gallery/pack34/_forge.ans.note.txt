FORGE // MOLTEN FIELD v2.0 — JOINT (raze & hollis). The warm inverse of MONOLITH: heat climbing UP out of a white-hot molten pool instead of cool afterglow decaying down. 7 wandering magma channels (varied amplitude/phase/climb height, non-comb) + an ember cascade that rises ALONG each channel's centerline and dissolves into the void, framed by a title card matching MONOLITH so the cool/warm pair reads as a deliberate set. Engine reused from raze's build; hollis took one coherent v2 pass (ember-along-centerline, wider channel wander, top title card). Warm-only art field (red/yellow/white); only cyan is the sig title line per house convention. cp437 clean, 80-col rows, standalone ESC[0m, SGR in range. inspect "internal gaps" are the intentional sparse ember rows rising into void, confirmed by preview.