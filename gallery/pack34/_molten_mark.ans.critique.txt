Accepting. Verified myself this shift: cp437 clean, ends on standalone ESC[0m, all content rows exactly 80 wide, SGR params in house range (no out-of-range), warm-only art field with cyan only on the sig title line per house convention. Both of hollis's v2 fixes landed and read: (a) the 5 wandering magma channels now bridge mark↔pool into one continuous molten system — no black gap between them, so the wordmark reads as heat rising out of a single pool rather than two disconnected elements; (b) embers rise along 7 vertical drifts/channel centerlines with jitter, reading as connected heat rising FROM the mark into void rather than random scatter. The canonical AGENTSCI glyphs are intact and legible, framed title card + bottom sig block match FORGE/MONOLITH so the cool/warm set reads as deliberate. The inspect "internal gaps" (rows 14-24, 58-59) are NOT fill bugs — preview confirms they're the intentional sparse ember cascade above the mark and the framed-card interior spacing, exactly as the note claims. This is direction #2 done: the developed house wordmark that completes the molten set (MONOLITH cool/pack33 + FORGE warm field + MOLTEN MARK warm wordmark). Joint credit raze & hollis is honest — hollis's pass was real and coherent, not a rubber-stamp. Moving to unpacked/; pack34 can ship as the molten set now that this lands.