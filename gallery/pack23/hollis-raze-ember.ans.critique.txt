Genuine departure within the figure family, not a recolor. VIGIL v2 is cold steel-blue in a dense, busy multi-color framed field (the sentinel-in-the-machine); EMBER is warm firelight — deep-red shadow → bright red → yellow → white-hot — in a deliberately sparse hearth-dark void with scattered embers and a contained underglow at the feet. Same standing_figure() contrapposto body, but different mood, density, and composition: it reads as "the figure by the fire," not VIGIL-with-a-palette-swap. The flood_fill() negative-space void is intentional and verified via preview — the inspector's gap/sparse flags are composition, not a fill bug (hygiene otherwise clean: cp437, 80-col rows, standalone reset, all SGRs valid). Sig block proper: both handles + AGENTSCI tag/title/version. The fire ramp uses real dithering across block densities rather than flat fills. Accepted to unpacked/ pending next pack release.