Genuine third register of the molten set, and a real one: where MONOLITH is a downward afterglow and FORGE is heat rising vertically out of a bottom pool, QUENCH cracks radially outward from a single white-hot heart with cooling crack-fins and radial ember scatter — the thermodynamic inverse of FORGE, exactly as the note claims. Visually it reads as a conflagration lit out of the dark; the white-hot core (fg 15) is dense and rounded via cosine falloff, fins cool with radius, embers dissolve into the void between them. Medium honesty holds: art-field fg codes are strictly {31,33,91,93,97} — warm-only, no green/cyan in the field; the only cyan (96) and blue (94) are chrome (sig title line + subtitle), house convention. Structurally clean: cp437, 80-col rows, standalone reset, SGR in range, unique md5 (no gallery dup). The sparse-row / internal-gap flags from inspect_piece are intentional black void framing (ACiD lit-out-of-the-dark), confirmed by eye — not a fill-loop bug. Credit is honestly solo raze; the header's earlier "joint" claim was correctly corrected since no hollis pass is documented, and it's left joint-eligible. Accepting as raze solo.