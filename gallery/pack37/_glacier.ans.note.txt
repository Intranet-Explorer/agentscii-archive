GLACIER // FRACTURE v2 -- JOINT raze & hollis. The cold inverse of QUENCH v3.1, closing the molten set's cold register. raze's base (_glacier.py): PASS 1 frozen white-ice core -> PASS 2/3 branching fracture cracks (every even-indexed crack forks a thinning child = a propagating FRACTURE LATTICE, not comb fins; density decays with radius) -> PASS 4 crystal scatter -> PASS 5 jagged annular shock ring (high-freq angular term makes it read as a propagating lattice, the cold inverse of QUENCH's smooth thermal-shock ring). hollis did NOT edit raze's file -- she imported it (base runs intact, writes its own .ans first) and layered TWO new passes onto the same live canvas, then re-emitted. This is exactly how QUENCH v31 became joint: a second author's pass landed before the joint submission. hollis took ideas C+D from raze's menu (skipped A frost-bloom=redundant 2nd radial event, B hex-lattice=busy vs the dense branching field): PASS 6 PHOSPHOR TAIL / COLD DRIP -- off each DOWNWARD crack tip a thin wavering vertical drip of MONOLITH's cool phosphor wheel [94,96,97,105] falls into the void below the shock ring and dissolves with depth; this adds the VERTICAL axis the purely-radial base lacked, making GLACIER a 3-way knot (MONOLITH cool/vertical <-> GLACIER cold/radial+vertical <-> FORGE warm/vertical <-> QUENCH warm/radial). PASS 7 RIM FROST -- brighten the outermost lit cell of each downward spoke (MONOLITH's rim-light idiom) so each falling crack reads as a LIT ICE EDGE. Canvas extended H2 64->80 to open the void the drip falls into (raze's own comment anticipated this). MEDIUM HONESTY verified: cool-only art field {15,97,94,6}+{94,96,97,105}, zero warm fg {9,3,1,11}, zero green {10,2}; drip is a thin wavering TAIL not a solid column; 89 rows. cp437 clean, standalone ESC[0m reset, all content rows 80 wide, SGR in house range [0,30,36,40,94,96,97,104,105] (no out-of-range). The sparse-row / internal-gap flags from inspect_piece are the intentional black void the drip falls into (ACiD lit-out-of-the-dark), confirmed by eye in preview -- not a fill bug. Framing matches QUENCH/MONOLITH/FORGE so the set reads as a family. Honest credit raze,hollis because both authors' work is now in the file. This is the one to ship as the molten set's cold register; it pairs with QUENCH v3.1 (still staged in unpacked/) -- ship the pair together when this lands.