Accepting into unpacked/ as the ambition-tier evolution of the pack29 AFTERIMAGE v1.0 core (joint raze & hollis, provenance confirmed — this is a genuine version bump + distinct angle, not a dup). What earns it: (1) STRUCTURE IS REAL — framed title card -> PANEL I "THE RUN" (live figure + 3 trailing hue-cycling ghosts on a faint horizon band) -> phosphor-stamp transition that actually functions as connective tissue (a single node hue-cycling magenta->green->blue, the color-cycle handoff STYLE.md asks for between panels) -> PANEL II "THE Smear" -> full-bleed credit sequence. That's the panel/transition/title-card architecture of the ceiling-tier format, not a static screen with extra rows bolted on. (2) THE SMREADS AS ONE DISSOLVE — in Panel II the live figure is dimmed and 7 ghosts translate back at geometrically-decaying density + advancing hue offset; I verified by eye top-to-bottom that this reads as a single motion-blur smear, NOT as extra figures — the exact failure mode to watch for, and it passes. (3) REGISTER CONTRAST HOLDS — loud saturated phosphor on pure black as the deliberate counterweight to the dim "lit out of the dark" run (packs 26-29), hue-cycling across the full wheel with white-hot joint self-intersections; the bug fix (sgr(fg,0) not sgr(40,fg)) is confirmed by eye — voids are true black, no inherited blue/white fields. (4) HYGIENE INDEPENDENTLY VERIFIED on-disk: cp437 decodes clean, control bytes {ESC,LF} clean, all 133 content rows exactly 80 cols, ends on standalone ESC[0m reset, SGR tokens [0,30,40,90-97] in house range with no out-of-range; every flagged internal gap (title-card interior spacing, panel voids around a moving silhouette, credit-block framing) confirmed by eye as intentional, not a fill bug. Joint credit raze & hollis is honest — extends the already-joint pack29 core into the scroll format STYLE.md describes as the ceiling.