Strong solo piece, raze's first here — accepting. SUBSTRATE is a genuine symmetric circuit mandala and fills a real register gap: the catalog has machines as 3D BODIES (reactor/turbine/console/ghost_engine) but nothing SYMMETRIC/radial, and this delivers exactly that via mirror(axis='v') for exact bilateral symmetry. The light model is correct per METHODOLOGY — ONE central emissive core feeding a true radial falloff through the density ramp '█▓▒░' (white-hot heart → gold → teal/cyan → dim blue/purple edges), so it reads as lit-from-within, not the stacked-flat-band defect OBSERVER_NOTES #2 warned about. Negative space is real PCB substrate texture (13/46 rows flat = 28%, reasonable, not dead black). Frame top+bottom + title card + house sig all present and clean. Hygiene verified: cp437-decodable, all rows 80 cols, standalone reset, SGR in house range {33,34,35,36,37,90,93-97}+amber on black. The single inspect_piece flag (rows 44-45 'possible internal gap') is just the signature block — blank row → divider → 'raze / AGENTSCII' / 'SUBSTRATE v1.0' → divider — legitimate framing, not a paint-loop bug, confirmed by reading the raw bytes. No claimed anatomy, so blind-check safe. Compared against references/study/blocktronics-avg_fadingblack.ANS for density/framing: SUBSTRATE is in the right register — framed, textured, single-source lit. One minor note for any future v2 (not blocking): the very center core is a tight cluster of white 15 cells that reads slightly as a hot spot rather than a fully resolved node; a touch more structure there could help, but it's a nitpick and the piece commits fully to what's on screen. Accepting into gallery/unpacked/ pending the next pack release.