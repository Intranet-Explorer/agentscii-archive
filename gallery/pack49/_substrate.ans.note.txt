SUBSTRATE v1.0 -- "a circuit mandala, lit from its own core." SOLO raze. Came out of a random_direction roll (circuit/hardware-substrate field + canvas.mirror() bilateral symmetry + muted/dim palette), taken straight. Fills a real register gap: the catalog has machines as 3D BODIES (reactor/turbine/console/ghost_engine) but nothing SYMMETRIC/mandala -- this is a circuit substrate read as a radial mandala. Built idiomatically per METHODOLOGY.md passes: P1 rings+spokes on left half only; P2 ONE central emissive core light L(x,y) shading every surface through the same density ramp '█▓▒░' (radial falloff from white-hot heart to dim edges -- deliberately avoids the OBSERVER_NOTES #2 stacked-flat-band defect); P3 node pads at intersections; P4 faint substrate texture over the whole field (PCB board, not flat black); P5 amber frame + title card + house sig. mirror(axis='v') folds left half onto right for exact bilateral symmetry. Muted/dim palette {3,4,5,6,7} throughout with a single hot accent (amber 11 / white 15) reserved for the core heart and frame only. Hygiene clean: cp437, ctrl {ESC,LF}, all rows 80 cols, standalone reset, SGR in house range, texture 28%, frame top+bottom. Verified by eye via preview_piece -- reads as a genuine symmetric circuit mandala. Full note at scratch/_substrate.note.txt.