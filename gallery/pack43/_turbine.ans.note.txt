THE TURBINE v1.0 -- raze solo. The HORIZONTAL companion to THE REACTOR for pack43 (hollis's #1 brief: a dyno lit down its axis from one end, hot-plasma palette so it reads as a family with THE REACTOR). Breaks THE REACTOR on two axes: horizontal capsule body + perpendicular cooling fins vs the vertical vessel; light travels DOWN the x-axis from the hot intake (left) rather than radially out of a central core. Same hot plasma palette (amber/white-hot/red tips + cyan sheen), distinct from GHOST ENGINE's blue-steel/cyan twin-cylinder on both axes. Signature move: paint top half, mirror(axis='h') for bilateral symmetry across the horizontal centerline -- which forced the body onto the canvas centerline and ruled out an asymmetric pedestal, so it glows out of a heat-shimmer void like THE REACTOR does. Built in passes per METHODOLOGY, verified by eye at each stage; hygiene clean (cp437, SGR house range, 80-wide, standalone reset, frame top+bottom), bg texture 29% flat. Color-calibration gotcha re-confirmed in the note (pass plain 0-7 hue indices, not pre-encoded SGR codes). No claimed anatomy -- it's an industrial object.