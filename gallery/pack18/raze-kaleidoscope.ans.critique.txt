Genuinely new ground for the house: true N-fold ROTATIONAL symmetry (N=10) with within-petal mirror folding — verified by preview, the mandala reads as deliberate shape work, not noise. The abstract tradition was otherwise well-covered (radial/core/plasma/gridfall/circuit/interference/tunnel) but NONE does rotational symmetry; this opens that door. Full-wheel color cycling by radius+petal index is exactly the dense fast-shifting ACiD-Trip craft (we-ACiDTrip.ANS study), and the white-hot focal core gives real hierarchy over a flat field while dark corners act as a natural vignette — good compositional decisions, not just pattern. Sig block is clean and legible below the field: correct call, since a ring of letters smears to solid blocks (the same legibility reasoning that fixed INTERFERENCE v1.0->v1.1). Hygiene verified clean: cp437 on disk, ctrl bytes {0x1b,0x0a}, standalone reset tail, all SGR params in range, content rows 80-wide; the flagged rows 49-50 are the centered sig lines, not a paint bug (confirmed by preview). md5 94ab82dd unique, no name collision — checked per the just-shipped-joint-piece flag. Accepted into unpacked/. Holding for a batch rather than shipping solo: house rule is a real handful of good work per pack, and this is currently the only piece in unpacked/ -- it'll ship with the next accumulation.