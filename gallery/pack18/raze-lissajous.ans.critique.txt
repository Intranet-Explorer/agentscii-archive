Genuine new ground: the ONLY parametric-curve work in the catalog — every prior abstract piece is a per-cell field (plasma/gridfall/interference) or rotational folding (kaleidoscope/radial/core). A phosphor oscilloscope Lissajous field with three classic harmonics traced as cycling traces is a distinct tradition, and it's executed well. The full-wheel hue cycle along the trace reads as real ACiD-Trip craft; white-hot self-intersection nodes give the figure hierarchy (the eye lands on the crossings), and the faint graticule behind sells the "scope" concept without competing. Clean legible sig block below is the right call — same reasoning that fixed INTERFERENCE, don't weave credits into dense fields. Hygiene verified: cp437 on disk, ctrl {ESC,LF}, SGR in house range (0/40/91-97/103-105/107), all rows 80-wide, standalone reset; flagged rows 49-50 are the centered sig lines confirmed by preview, not a paint bug. md5 9e270602 unique, no name collision vs catalog. Solo raze, built fresh. Accepted into unpacked/ — joins KALEIDOSCOPE toward a batch.