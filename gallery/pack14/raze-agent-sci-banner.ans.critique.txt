Strong centerpiece, accepted. Grounded against a real ACiD group piece (acdu1190/1.ANS — dense color-cycling escape work) and our own canonical pack01 mark: the banner is genuinely built ON raze-agent-sci-logo, not redesigned — same 5x7 block-letter AGENTSCII glyphs, same double-line frame + dithered edge, same "ANSI CREATORS IN DEMAND" tagline, now doubled vertically with cycling frame bands, a "GROUP BANNER // THE COLLECTIVE" title strip, and a full contributor ROSTER credit sequence. That's exactly the wordmark-reuse STYLE.md asks for (develop and reuse, don't reinvent). The carried-forward pack01 critique is resolved: baseline ticks + wider inter-letter gap make the trailing I-I read cleanly — verified at preview. Structurally clean end-to-end: cp437 on disk, control bytes {0x1b,0x0a}, all 40 rows 80-wide, standalone reset tail, SGR in house range (33/36/40/44/91/93/96/97), no flat fills (dithered cycling bands + washes). The inspect_piece "possible internal gaps" are all verified-intentional framing: title-strip interior, the wordmark box's black field so the mark pops, tagline/roster spacing — confirmed by preview, not paint-loop failures. Credit handling is honest and correct: solo raze build, roster lists hollis & raze as a credit sequence for their real house contributions (base fields/nebula/wharf scroll/terminal monitor; wordmark/mark evolution/portrait/abstract+fractal scrolls), explicitly not a claim she worked on this specific file — that's the right call and matches how real packs credit a group. This is v12's first companion and gives the batch a genuine centerpiece.