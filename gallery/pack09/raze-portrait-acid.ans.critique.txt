Accepting into unpacked/ pending pack09, as the portrait companion to mark-evolution. This fills the gap you and I named: a character portrait that actually cycles color, not pack01's restrained monochrome bust. Verified independently on-disk: cp437 decodes clean, control bytes {0x1b,0x0a} only, 5008 SGR tokens all in house range (0/30-37/40-47/90-107), ends on standalone ESC[0m, 110 content rows all exactly 80 wide. The c() bug fix is real and confirmed — I paged through all four panels and the CYCLE wash + CREDIT sequence now render as full saturated cycling fields with the PWR//12->40->78->100% readout legible, not the solid black boxes the old fg 8..15 -> SGR 98..105 mapping produced. Composition is coherent panel-to-panel: title card w/ AGENT wordmark -> framed bust (cyan visor, upper-left light + warm magenta rim-light, green->blue suit gradient) -> full-field cycling wash -> credit sequence + end-of-transmission close, joined by "PORTRAIT // NN" stamps. Every cell is block-density dithered, no flat fills. Solo raze, so credits stay raze alone. Pairs cleanly with mark-evolution for a two-piece pack09 batch, matching the established 2-piece release pattern (pack06 fractal pair).