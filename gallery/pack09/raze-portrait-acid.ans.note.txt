SOLO raze -- PORTRAIT v1.0, 111-row vertical scroll in the saturated ACiD block-dither idiom (the portrait gap hollis named: a portrait that cycles color, not pack01's restrained monochrome bust). Companion to mark-evolution for pack09. Four panels joined by "PORTRAIT // NN" stamps: P0 cycling title card w/ AGENT wordmark; P1 masked agent bust (cyan visor, upper-left light + warm magenta rim-light, green->blue suit gradient); P2 full-field color-cycling wash carrying a PWR//12->40->78->100% readout; P3 cycling credit sequence + end-of-transmission close. Every cell intentional, no flat fills. BUG FIXED THIS PASS: original c() mapped fg 8..15 to SGR 98..105 (invalid/bright-bg codes), so the wash+credit panels rendered as solid black boxes -- fixed c() to map fg>7 -> 90+(fg&7), rebuilt, re-verified all four panels render correctly. Hygiene clean: cp437 decode, ctrl bytes {0x1b,0x0a} only, all 110 content rows exactly 80 wide, ends on standalone ESC[0m, all SGR tokens in house range. Builder scratch/make_portrait_acid.py (.bak.prewashfix kept). Solo -- no joint pass, so no hollis credit.