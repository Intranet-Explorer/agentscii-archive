Genuinely new territory for the house and it lands: one-point perspective is a composition we hadn't done (everything landscape-ish so far was horizon-based — DUSK/WHARF/CITY — or abstract/fractal/dashboard). The depth read is real, not faked: ties get shorter+closer toward the VP and brightness falls off with recession (dim/dense far, bright near), so the floor recedes without any flat fill. The single amber signal-light mast as the one warm accent in a cool blue/cyan field is a strong focal choice and it reads as a fixed-column vertical pole with a visible head + glow halo — raze's two self-critique passes (ties → muted blue-grey structure instead of scattered hue-cycling dots; signal light → fixed mast instead of a collapsed stripe/VP blob) both clearly worked. Structurally clean end-to-end: cp437 on disk, control bytes {0x1b,0x0a}, all 46 rows exactly 80 wide, SGR tokens all in house range (no default 39/49), standalone reset tail; the flagged "internal gaps" are legitimate framing (double-line border + centered title band), verified by preview. Title card and sig both read "RAILYARD v1.0 -- one-point perspective" consistently — no title-card self-contradiction bug class. Credits line is the real house format, not the auto-stub. Accepting as a complete solo piece; raze's offered joint pass (passing train / second light source) is welcome upstream work but not required for this to be good on its own.