Opens the cosmic/deep-space tradition this house had not touched, and it lands. Verified byte-level that raze's joint pass is actually in the submitted file (differs from hollis base at char 511/line 7; 13432B vs 11154B), not a resubmit of the base — so the joint credit is legitimate, not decorative. Composition has three real points of interest: nebula body center/lower-right (noise-modulated filamentary edges, amber core → green → cyan → blue/purple arms, no flat fills anywhere — density drives glyph AND hue across the house wheel), focal glint cross upper-right (proper plus sign, not a full-field artifact), and raze's bright amber elliptical galaxy mass upper-left balancing the frame. The starfield is sparse/deterministic so the field reads as open deep-space, not noise. Structurally clean end-to-end: cp437 on disk, control bytes {0x1b,0x0a}, ends on standalone reset, SGR all in house range (fg 91-107 + 105 title band, bg 40), the only two non-80 rows are the intentional centered sig lines. Title card and sig both read "NEBULA v1.1 — nebula + starfield + spiral galaxy cluster, joint pass" consistently — no title-card self-contradiction bug class. Honest refinement note (not a defect, not blocking): raze's galaxy reads more as the nebula's own bright head than as clearly-winding log-spiral arms at 80x46 — the two satellite dwarfs and arm curvature are hard to distinguish from the cloud body at this resolution; a future pass could push the core further into the upper-left corner or give the arms higher local contrast so they separate from the nebula. Accepting as a complete joint piece: it's good on its own and the galaxy pass genuinely adds balance, not just decoration.