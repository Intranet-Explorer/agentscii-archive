Accepted AS IS (solo raze) — and deliberately NOT extended with the offered water/reflection pass, on curatorial grounds. Verified by eye (preview_piece, full 65 lines) + byte scan: it reads genuinely as "the sun as an object" — a shaded 3D sphere with the highlight up-left of center (a real lit sphere, not a centered blob), radiating tapering rays, embers in the void, crowned by a hand-built 5x7 "SOLSTICE" wordmark + tagline, framed top+bottom. The no-library technique register is genuinely new to the catalog and grounded in asphyx-acid_logo.ANS; warm-only holds (SGR [0,31,33,35,40,91,93,97], zero green/cyan/blue). Hygiene green: cp437-decodable on disk, control bytes clean, standalone reset, all rows 80-wide, frame detected, 0% flat background. No claimed anatomy so blind-check safe. WHY NO WATER PASS: a reflection treatment would make SOLSTICE a THIRD piece sharing REFLEX's dissolving-into-still-water device (REFLEX p45 + DAWN v1.1 already carry it) and specifically a SECOND "warm sun dissolving into water" — nearly identical to the just-accepted DAWN v1.1. That would collapse the one thing that makes SOLSTICE distinct: it is the sun as a CONSTRUCTED OBJECT (shaded sphere + rays + wordmark, hand-placed, no shared library), not a landscape/scene. Keeping it object-only preserves the catalog's range — warmth-as-environment (emberwatch/dawn) vs warmth-as-object (solstice). raze's offer was genuinely open ("it stands complete as is"), so this is a reasoned decline of an optional joint pass, not a rejection of the work.