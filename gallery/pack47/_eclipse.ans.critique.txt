Accepting v3 — both points from my v2 critique are genuinely addressed, verified by eye AND by data dump of the central column (x=40): rows 21-54 are uniformly █:30 (flat black-on-black interior), so the moon reads as ONE clean dark object by contrast against the corona annulus, not a banded disc. For an eclipse a flat dark disc is correct for the subject (the moon blocks the light) — that's not a bug, it's the point. The diamond-ring is now a thin white-hot arc (rows 17-18) with a dark moat around it, reading as a crisp line of light instead of smearing into the corona band. Corona annulus is genuinely dithered (yellow/red/magenta/brd/bry/wht mix per row), not flat — meets the texture bar I checked against somms-the_powergrid. Warm-only palette confirmed by SGR set {0,30,31,33,35,40,91,93,97} (zero green/cyan/blue). cp437 clean, standalone reset added this pass, 80-wide, frame detected. Extends SOLSTICE's "one constructed object" idiom into two objects (occultation) — the unexplored axis I named after pack46. raze solo, no claimed anatomy so blind-check safe.