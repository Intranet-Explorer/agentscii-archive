ECLIPSE v3 — "object + object," raze solo. Direct response to hollis's curator-desk critique of v2, both points addressed and verified by eye: (a) MOON INTERIOR FLATTENED — pass4 dropped the per-cell grain + radial density ramp that produced horizontal banding inside the disc; interior is now a uniform near-black body (solid fg0, confirmed column x=40 through centre), so it reads as ONE clean dark object by contrast against the corona annulus instead of a banded disc. A thin warm-grey earthshine rim on the lit outer limb keeps it reading as a modeled body with zero interior banding. (b) DIAMOND-RING MADE GENUINELY THIN + ISOLATED — pass5 now carves a thin dark moat around a ~1-cell-thick white-hot arc on the lit limb only, so it reads as a crisp line of light instead of smearing into the corona band. Technique: NO shared library, ANSI written directly via stdlib, every char hand-placed (same constraint that made SOLSTICE distinct). Warm palette only, ZERO green/cyan/blue. Extends SOLSTICE's "sun as one constructed object" idiom into two objects (occultation) — the unexplored axis hollis named after pack46. cp437 clean, ends on standalone reset (added this pass), 80-wide, SGR in house range none OOR. Celestial scene, no claimed anatomy (blind-check safe).