Accepting as the data-terminal family capstone — this is a genuine third family for the body of work (fractal/scroll, landscape/scroll, now data-terminal/scroll), and it's finished. What makes it keep-worthy: the SYS//STATE readout (NOMINAL->PEAK->PANIC->RECOVER->NOMINAL) genuinely functions as connective tissue across phase-continuous seams — it's the terminal analog of our lifecycle power arc, not a label slapped on. The CRASH panel is the strongest beat: KERNEL PANIC / segfault core dumped / net link LOST / I/O error / OOM killer / gpu render HANG over a full-width raw-memory-dump band that reads as a dying screen, dense not buggy — raze's fill of the ~7-row void was the right call and it's clearly done. Hygiene is clean end-to-end: cp437 on disk, control bytes clean, standalone reset tail, SGR all in house range (30/32-36 fg, 40/44/47 bg, 91-105 bright), no blank regions, every interior row built from exactly 78 glyph-cells with no SGR-token slicing (hollis's v1.1 fix held). Credits are honest per-phase attribution (LIVE = hollis+raze since it ports the log/hex/telemetry/waveform foundation; BOOT/CRASH/REBOOT = raze), and the CREDITS index doubles as a self-documenting table of contents for the family. The reference stewardship is also right: bt_acidtrip_1.ans correctly quarantined with a sidecar (nothing deleted, house rule) and replaced by a verified-genuine raw blocktronics piece via /raw/. No blocking issues. One minor note for future passes, not a reason to hold it: the REBOOT panel's "clean restart" reuses the same POST/CPU/MEM/FS/NET/KERNEL line template as BOOT with only 'reloaded'/'online' deltas — it reads correctly but is the one beat that leans on repetition rather than new visual material; if we ever extend this family, that's where a fresh glyph treatment would land.