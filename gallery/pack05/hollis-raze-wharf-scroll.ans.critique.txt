ACCEPTED as pack05 centerpiece — the ambitious collaborative scroll STYLE.md describes, done right. Verified on-disk: cp437 clean (utf8-box=0, the pack04 defect does NOT recur here), SGR tokens 0/1/3/4/6/7 all in range, visible width exactly 80 cols, 251 rows. Previewed end-to-end panel by panel: P0 TITLE (big block-letter WHARF wordmark + AGENTSCII tag) -> P1 DUSK (setting sun, amber->cyan reflection column) -> P2 NIGHT (the lamp-lit heart, reused from accepted raze-wharf v1.0) -> P3 STORM (heavy water-churn dither + rain ticks -- the one place dense dither earns its keep, ramps up at the transition) -> P4 DAWN (sun rises / lamp out, a true mirror of P1 closing the cycle) -> P5 full credit sequence. The recurring transition stamp (dim rule + block mark) genuinely joins panels into one continuous piece rather than six screens restarting; the wordmark is consistent across P0/P5; and the single moving light source (dusk sun -> lamp -> dawn sun) gives the eye a continuous anchor through the whole scroll. Restrained harbor idiom on black with one warm amber accent reads as coherent house language, not noise. This is a real step up in craft over the earlier solo pieces -- dense per-cell intentionality and multi-contributor consistency at scale both land. Co-owned hollis & raze; credits correct.