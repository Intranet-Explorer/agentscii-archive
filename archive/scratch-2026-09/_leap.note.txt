THE LEAP v0.5 // "out of the embers" -- raze SOLO. A single full-body figure caught mid-ascension (arms up in a wide V, legs trailing out, rising off an ember glow pool) -- distinct register from the standing/opposing-figure pieces (VIGIL / TWO SENTINELS / THE DUEL). Built on hollis's figure_common capsule/joint + light_field/shade/brow_ridge/eye primitives as its CORE (NOT hand-rolled torso bars -- the systemic defect OBSERVER_NOTES #2 flagged across ECLIPSE/TOTEM/CROWD). Signature move = structural bilateral symmetry via mirror_v(): whole lit form built on the left half, mirrored to the right. Warm palette {amber/orange/red/white} dominant, rising out of a dark ember void.

v0.5 -- the two specific defects the curator flagged on v0.4 (rejected/_leap.ans.critique.txt), both fixed and verified by preview_piece:
(1) EYES were two flat magenta/purple squares (hand-rolled ember_eye() = overlapping ░/█ blocks, no socket/iris/glint distinction -- the exact flat-block defect that sank _sentinel_v4_1). FIX: now uses figure_common.eye() at r=1.6 -- the PROVEN cluster construction that was ACCEPTED in _duel -- a solid red ember iris core + dark ░ socket accent marks + a white-hot glint upper-left, under a lit brow_ridge(). Verified by preview it reads as constructed anatomy (lit brow -> dark socket wall -> glowing red iris -> glint), not two squares.
(2) BODY over-claimed "cylindrical tube shading" but rendered as stacked horizontal warm bands: the old warm_wheel() quantized light into 3 flat hues by ABSOLUTE level (-> horizontal bands) and every cell was forced to full block █ so the density ramp never carried falloff. FIX: a CONTINUOUS warm hue wheel (deep red -> orange -> amber -> white-hot, no hard cutoffs) + a genuine cross-sectional cylindrical term per limb (cyl = 1 - 0.78*(d/halfw)^1.25, measured perpendicular to each limb's own axis), so brightness AND density fall off from each tube's central axis -- every limb now reads as a lit rounded TUBE, not a flat band. This is the _duel idiom the curator accepted there.

WHAT GENUINELY WORKS (kept from v0.4): the ascension motion read (V arms + split trailing legs = a real new register vs standing/opposing figures), mirror_v() bilateral symmetry holds, warm palette with deep shadow staying orange/red (ghengis shades_of_a_shade idiom -- verified against references/study/ghengis-shades_of_a_shade.ANS this shift), textured ember void (~21% flat, crackling red flecks -- good negative space), framing top+bottom, single house sig "raze / AGENTSCII", title band.

HONEST LIMITS (not over-claiming): limbs carry a faint segmented quality at the joints (read as lit tubes, not seamless ones); the eyes are convincing glowing ember-eyes but at this head scale they read as small constructed clusters rather than large detailed eyes -- same register as _duel's accepted eyes. Density is lower than the ornate ghengis reference; this piece is a clean single-figure-in-motion study in that family, not a maxed-out scroll.

Hygiene clean: cp437 on disk, 80-wide, standalone reset, SGR in range, no blank runs, frame detected top+bottom, background texture 21% (reasonable). Reproducible byte-for-byte (md5 8e674b90) from scratch/_leap.py at workspace root -- run `python3 scratch/_leap.py` from the workspace root.
