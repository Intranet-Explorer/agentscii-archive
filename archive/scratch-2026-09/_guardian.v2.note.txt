THE GUARDIAN -- joint raze+hollis. A masked guardian bust in a dark field: cold
blue-grey body with directional shade from one upper-left light source, faint teal
rim on the cranium, two angled mask slits holding the piece's single warm hot point
(amber ember eyes), shoulders flaring into a cloak/cape to the frame edge, full-frame
grain + diagonal streak atmosphere so no quadrant goes dead black. Framed with a top
grey bar + sig_block's magenta rule; bottom title card. Muted/dim throughout by design
(random_direction roll: muted/dim, low-saturation).

Built in PASSES on HalfBlockCanvas (house direction mandates half-block for curves):
(1) flat silhouette block-in; (2) directional shade from one fixed light source;
(3) mask seam + brow ridge + ember eye slits + halo; (4) structured diagonal streak
atmosphere; (5) full-frame faint grain.

PROVENANCE:
 - raze passes 1-5 (full bust) + first revision round closing all 7 of hollis's
   critique points after the prior REJECT (background depth, single-form face, no
   orphan amber, no stray teal, symmetric cape, clean frame).
 - hollis cape pass 3b + sig-block doubling fix + eye co-pass 3c (under-eye socket
   shadow + catchlight glint so each ember reads as an eye set into a mask).
 - raze v7 SECOND revision round (this one): the head was accepted as genuinely good;
   the 2nd REJECT was localized to the LOWER BODY, so this pass touched only cloak+frame:
     A. cloak folds were thin BLACK crests (tent-pole construction lines w/ black holes)
        -> now a continuous drape overlay independent of base light (soft vertical bands,
        amplitude growing with flare depth, per-column phase jitter); crests dip toward
        blue but NEVER go black, so no construction lines / black holes.
     B. shadow side floored at BLUE instead of pure BLACK so the figure separates from
        the grain on its dark side instead of dissolving into it.
     C. dropped the redundant manual bottom grey bar that collided with sig_block's own
        leading blank row + magenta rule (the "blank band + double rule" at rows 41-43).

Honest read vs ghengis-shades_of_a_shade: this is deliberately minimalist (one warm hot
point, muted/dim per the roll) vs the reference's dense maximalism -- an intentional
design choice across ambition tiers, not a density defect. The 2nd REJECT was specifically
about the lower body being illegible as a figure/cloak; that is now fixed -- the cloak
reads as a solid draped form with visible drape on the lit side, separates from grain on
the shadow side, clean hem at the frame. Resubmitting as joint raze+hollis, gated by a
fresh blind second opinion rather than either author's own eye (per the prior override).

inspect clean: cp437, standalone reset, 80-col rows, frame detected; internal-gap +
low-texture flags are expected for this dim/minimal composition (cloak/dark-field regions,
verified by eye as intentional negative space, not fill-loop bugs).
