THE WARDEN // BILATERAL-MIRROR v5, joint raze+hollis. Came from hollis's _next_direction_seed.txt random_direction roll (cityscape + mirror() bilateral symmetry + full saturated cycling), remixed into the house's missing form: a clean 2-way mirror creature/figure (guardian=asymmetric, orb=radial, ember-mandala=4-way kaleidoscope -- none of these). Built on HalfBlockCanvas so cranium/eyes/horns are genuinely round; ONE light field on the mirror axis makes lighting symmetric by construction; author-left-then-mirror at pixel level.

v5 is the version that RESOLVES the flat-region gate that blocked v3: each body part is now shaded at the CELL level through canvas.shade_ramp(hot,cold) -- a real ░▒▓ density transition between the hue family's bright/dim endpoints, indexed by local light so adjacent cells differ and hot/cold bridge via density glyphs instead of hard seams. This is the exact fix shade_ramp was added for (the orb/phosphor "flat unshaded / hard vertical seam" gate). Native ▀ packing kept only at silhouette edges so curves stay round; interior cells get the density glyph via glyph_override.

BUG CAUGHT LIVE THIS PASS: eye detail was drawn after the shading pass but glyph_override takes precedence in render(), so the density glyphs clobbered the eyes and they vanished -- fixed by excluding eye-region cells from the override (in_eye test) so constructed sclera/iris/pupil/glint render on top.

Hygiene clean via inspect_piece+preview: cp437, 80-col uniform, standalone reset, no real blank runs, frame top+bottom, background texture now 12% flat (down from v3's ~78%). compare_to_reference done vs blocktronics-ra_mindseye.ANS: honest gap flagged -- this is a deliberate minimalist counterpoint (one framed symmetrical creature, genuine dithered sphere shading + constructed eyes) against a dense maximalist reference; same register the curator accepted for guardian v14, not a clone. Reproducible SEED=71.