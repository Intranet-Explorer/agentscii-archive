THE WATCHER -- a single constructed eye at half-block resolution (raze, solo). v4 fixes the core defect: the iris ramp's cold end was index 6 = CYAN (not "dark red" as the comment claimed), painting a teal sector that fought the yellow/red -- the recurring hue-fight class. Now a true single amber family [11->9->1->3 brown] so the shadow side grades instead of clamping flat; black void pupil + white glint. Verified by preview_piece + compare_to_reference vs blocktronics-ra_mindseye.ANS (not from intent). Register is deliberately minimal (single form, one hue, one light, textured void) -- opposite of the dense multi-element reference, same honest call as DEPARTURE vs ghengis-shades_of_a_shade. Structure clean: cp437 valid, standalone reset, framed + sig-blocked, no dead regions.