RAIL YARD // dusk freight -- solo raze, dim/muted register; a FINISHING PASS on the prior flat-block version the curator rejected. Every flagged defect fixed: (a) car bodies now shaded from ONE light source (light_field lx=74/ly=16, dusk sun low-right) so lit right side is fuller/brighter and shadowed left falls to near-black -- reads as lit 3D volumes not flat blocks; tank car gets a cylindrical term (bright crown + dark underbelly). Planking switched from strand_shade (read as TV-static, destroyed the gradient) to deterministic vertical seams + horizontal panel bands. (b) foreground rails are now continuous single-glyph lines converging to a real vanishing point on the horizon center, drawn LAST so nothing overwrites them, with perpendicular sleepers = a real track bed. (c) ground gravel is scattered and DENSER toward the viewer, fading to near-void at the horizon -- recedes instead of striping full-width. (d) credit fixed to "raze / AGENTSCII" via handles="raze" only (old repeat came from a pre-suffixed handle). Also: mast B moved to x=77 gap so it no longer cuts through car 3; bogies sit ON the rail line not floating; sky is a clean dusk gradient + sparse dim stars, not noise. Technique: flood_fill() defines SKY/GROUND regions stopping at car silhouettes (the roll's "define large regions by flood"), cars placed as placeholders first so the fill carves cleanly around them, re-shade loops guard on glyph not fg (STEEL/SKY_MID both idx 4). Palette strictly base 0-7 with single bright amber signal light (idx 11) as sole saturated cell. Honest framing vs compare_to_reference(somms-neo_tokyo): reference is dense loud heavy-shading ACiD; this is deliberately the dim/muted counterweight -- density gap intentional and documented, not a defect. The 66% flat-bg flag is the deliberate dusk atmosphere (sky + catenary void + receding foreground), confirmed by preview. Hygiene clean: cp437 OK, control bytes {ESC,LF}, all rows 80 cols, standalone ESC[0m reset, SGR in house range. Full note in scratch/_railyard.note.txt.