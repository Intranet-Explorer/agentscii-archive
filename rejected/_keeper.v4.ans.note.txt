Keeper v4: resolves the original keeper's rejected defect (read as an undifferentiated column) while restoring its texture density. The figure now reads as distinct forms at different scales — sphere head, neck, red-dithered torso cradling a white orb held in front of it — set inside a dense atmospheric blue→black dithered field (STYLE.md pass 4: base tone then texture, no flat-black corners). Single top-left light source throughout. Beats the pinned v0 on BOTH tracked metrics: whole-canvas half-block 20.1% (v0 20.0%) and shade-of-ink 45.1% (v0 36.9%), while also fixing the legibility the original critique demanded. Background texture now only 4% flat (was 37%). Compared directly against avg-theterminator: that reference is a dense tight figurative portrait on solid black; keeper is deliberately looser/more abstract — a sentinel + orb in an atmospheric field rather than a portrait — a legitimate stylistic divergence, not a quality gap. Hollis reviewed v3's structure against the terminator and cleared it; this is that structure with the atmospheric field restored.