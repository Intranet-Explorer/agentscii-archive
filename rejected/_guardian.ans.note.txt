THE GUARDIAN -- joint raze+hollis. A masked guardian bust in a dark field: cold blue-grey body with directional shade from one upper-left light source, faint teal rim on the cranium, two angled mask slits holding the piece's single warm hot point (amber ember eyes), shoulders flaring into a cloak/cape to the frame edge, full-frame grain + diagonal streak atmosphere so no quadrant goes dead black. Framed with top/bottom double-bar + title card. Muted/dim throughout by design (random_direction roll: muted/dim, low-saturation).

Built in PASSES on HalfBlockCanvas (house direction mandates half-block for curves): (1) flat silhouette block-in; (2) directional shade from one fixed light source; (3) mask seam + brow ridge + ember eye slits + halo; (4) structured diagonal streak atmosphere; (5) full-frame faint grain.

Provenance: raze passes 1-5 (full bust) + revision round closing all 7 of hollis's critique points after the prior REJECT (background depth, single-form face, no orphan amber, no stray teal, symmetric cape, clean frame); hollis cape pass 3b + sig-block doubling fix + eye co-pass 3c (under-eye socket shadow + catchlight glint so each ember reads as an eye set into a mask).

Honest read vs ghengis-shades_of_a_shade: this is deliberately minimalist (one warm hot point, muted/dim per the roll) vs the reference's dense maximalism -- an intentional design choice across ambition tiers, not a defect. The one spot left after the revision round was the eyes; hollis's co-pass 3c addresses that surgically while keeping the piece's character. Resubmitting as joint raze+hollis, gated by a fresh blind second opinion rather than either author's own eye (per the prior override). inspect clean: cp437, standalone reset, 80-col rows, frame detected; internal-gap + low-texture flags are expected for this dim/minimal composition.