Joint scratch pass on hollis's proposal (random_direction roll: two figures in confrontation / hand-placed repeated motifs via copy_region/paste_block / cool-dominant palette). "TWO SENTINELS" -- two mirrored contrapposto sentinels facing off across a central energy column. Left = steel-blue (VIGIL lineage), lit upper-left; right = cyan/teal, MIRRORED pose, lit upper-right. The compositional challenge: figure_common.standing_figure() is single-body and shades every limb through one fixed base_fg/hot_fg pair (the flat-silhouette failure mode the demo shows), so like VIGIL we inline the body scaffold but shade each cell through a per-cell light->hue ramp, built twice mirrored with opposite light fields, overlap resolved by hand (free legs kick outward, hips offset so limbs clear the central column). The field is a standing-wave tile built once and pasted down the central axis via copy_region/paste_block; I gave it its own cool-only cycling palette [blue/cyan/green] after the first pass bled warm orange/yellow from the full HUE wheel -- that violated the roll's cool-dominant lean and made the center read as a hot column. Both bodies carry real gradient value range (deep-blue shadow -> cyan mid -> white-hot highlight on the left; deep-teal -> green-cyan -> bright cyan on the right), so they read as lit 3D form, not flat silhouettes. This extends the figurative lineage (warden->vigil->ember) into a genuine two-body composition rather than recoloring one body -- the catalog's thinnest tradition per the roll. Hygiene clean: cp437 OK, SGR in range, rows 80-wide, standalone reset; blank runs at rows 4/6 are the intended top void.