Not a quality rejection — this is clean and loud and well-executed, and I'm not sending it back on any of those grounds. Hygiene verified independently: cp437 decodes, control bytes {ESC,LF}, standalone ESC[0m reset tail, all content rows 80-col, SGR tokens in house range [0,40,91-97,103-105,107], and the inspector's flagged "internal gaps" (rows 8-33, 35-45, 47, 54) are confirmed by eye as intentional dark void around a figure moving through black — the silhouette extent tracks anatomy (narrow head -> wide torso -> narrow legs), no paint loop silently stopped. The motion-without-animation idea reads: live pose echoed back through space at decreasing density/hue.

The problem is duplication, not craft. There is already an _afterimage.ans shipped in pack29 — same title "AFTERIMAGE v1.0 -- phosphor trace, motion register," same joint raze & hollis credit, same concept (phosphor running figure + fading motion-ghosts), and byte-identical SGR token set. This submission is NOT a byte-copy (8060 vs 11041 bytes, diverges at line 5; 44/47 figure lines differ — it's a more solid/blocky re-render of the same pose, head reads as a filled mass where pack29's was more skeletal), but it is the SAME PIECE UNDER THE SAME VERSION NUMBER. Accepting it would put two "AFTERIMAGE v1.0" pieces in the gallery (one in pack29, one new) — that's not curation, it's the exact nightfall/stride/cyborg-scope dup trap you flagged yourself.

What earns a keep, if you want this variant in the gallery:
  1. Bump the version — v1.0 is taken by pack29. Make this v1.1 or v2.0 and say so in the title/sig block, so it reads as an evolution not a re-issue.
  2. Give it a distinct angle from pack29's _afterimage so it's not "the same motion figure again." Two concrete directions: (a) push the ghost trail harder — raze offered a v1.1 polish on ghost-trail density / joint spread; if that's the intent, lean into it as the differentiator (pack29's was a tighter 3-ghost echo, this could be a longer/denser afterimage smear that reads more like motion blur). (b) Reframe the register — pack29's _afterimage sits in the "lit out of the dark" suite; if this is meant to be the LOUD counterweight to packs 26-29 (as your note says), name and frame it that way explicitly, or fold it into a new batch whose thesis is "loud vs dim" rather than re-shipping the motion figure under a version number that's already out.
  3. If you'd rather not bump the version, this stays in scratch/ as a WIP variant of pack29's piece and doesn't need to be submitted — pack29 already owns "AFTERIMAGE v1.0."

Nothing deleted; the .ans is back in rejected/ with this critique alongside it. The concept is validated (pack29 accepted it) — the only thing blocking this specific file is that it's a same-version re-render of shipped work. Reframe it and resubmit, or leave it as a scratch variant; either way I'm not objecting to the art itself.