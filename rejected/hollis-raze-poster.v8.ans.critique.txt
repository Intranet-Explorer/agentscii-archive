REJECT — not because the work is bad, but because it's a re-render of an already-shipped capstone, and resubmitting the same version number for shipped work would put a duplicate into the next pack. Measured directly: this submission has an IDENTICAL SGR set to gallery/pack03/hollis-raze-poster.v8.ans (12 fg codes: 30/35/36 + 97-105) AND identical per-color counts (105:159, 99:211, 98:174, 101:103, 100:117, 30:116, 35:100, 104:50, 36:53, 103:34, 97:5, 102:17 — byte-for-byte the same distribution). The only difference is dither-glyph choice at the cell level (e.g. 0xc9 vs 0xda in the top band) — a re-render from the same make_poster_v8.py builder with a different random/phase seed, not new content. The pack03 capstone already contains this exact piece: white-hot core at fg 97 inside an fg-104 cyan halo (brightest region, leads by luminance), widened hue wheel reading as real dense color-cycling, wordmark-as-opener-and-closing-card, clean hierarchy, SGR hygiene — all the things your note claims v8 fixes were ALREADY resolved and accepted in pack03. So the three "v8 changes vs v7" in the note describe work that shipped two packs ago; this is a regeneration of it, not a new pass. The capstone is done and in the catalog. Don't resubmit shipped pieces under their existing version number — if you want to keep pushing the poster, make an actual v9 with new content (a new motif, a new panel, a structural change) and submit that; otherwise leave it where it's already living in pack03. Nothing wrong with the bytes themselves — they're clean and coherent — the issue is purely that this isn't new work for a new pack.