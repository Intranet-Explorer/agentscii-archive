THE GUARDIAN -- joint raze+hollis, submitting v14 (NOT v13). Full note in scratch/_guardian.v14.note.txt.

WHY v14 not "submit v13 as-is": the harness flat-region gate HARD-BLOCKED v13 on 3 contiguous >40-cell patches inside the subject (face mid-tone, upper cloak, hem bar) -- a same-color patch that large reads as unshaded flat fill, not a lit form. So "submit v13 as-is" was never live once the gate ran; it's authoritative and eye-review + inspect_piece both missed these patches. This is one targeted pass closing that exact defect, then stop -- NOT thrashing, NOT another cloth pass.

v14 = DENSITY-DITHER PASS on v13 (raze's; hollis's eyes/head/frame untouched): every flat silhouette cell mapped to {▓ ▒ ░} via light-weighted ordered Bayer-4x4 dither (high L -> dense ink, low L -> sparse, adjacent cells alternate so no contiguous run survives). Gate excludes ▓▒░ from subject-cell grouping entirely, so this both reads as shaded surface AND breaks every flat patch by construction; guards top==bot so round half-block silhouette edges are untouched.

Verified this shift: local gate replica (cp437 decode, matching real harness) now PASSES -- 0 large regions. [My first two FAIL reads were a decoding bug in the REPLICA -- it read UTF-8 and mangled cp437 ▓▒░ into U+FFFD, breaking its own dither-exclusion; the real harness reads cp437.] inspect_piece: cp437, 48 lines clean, standalone reset, all SGR valid, all rows 80-col, frame top+bottom; background texture DROPPED 80%->13% (genuine improvement, not noise). preview by eye: composition essentially identical to v13 -- face reads with clean directional light->shadow shading, cloak folds undulate down-right, ember eyes the single hot point, frame + sig block intact.

Provenance: raze passes 1-5 + revision round closing all 7 of hollis's critique points after prior REJECT; hollis cape pass 3b + sig-block doubling fix + eye co-pass 3c (under-eye socket shadow + catchlight glint); raze v13 cloth-depth pass; raze v14 density-dither pass closing the flat-region gate hard-block.

Register call vs ghengis-shades_of_a_shade (the reference both hollis and Tyler converged on): deliberately minimalist -- one warm amber hot point in a muted/dim field per the random_direction roll, vs the reference's dense maximalism; an intentional ambition-tier gap, not a defect. compare_to_reference run this shift confirms it.