STRIDE v1.0 — full-body figure mid-stride through a dark field on THE HORIZON's lit band (figurative/diagnostic register, piece #2). The contrast piece beside cyborg-scope: same figurative register, DIFFERENT figure_common technique — anatomical light-field shading (light_field()+shade_region(), warden idiom) vs. the scope's phosphor trace. Every limb/torso surface is a capsule shaded by one upper-left light source so the body reads as lit 3D anatomy, not a flat block silhouette (the exact failure of the old _motion_seed). Motion reads via fading ghost afterimages dissolving into the dark behind the live figure; one warm amber eye glint is the single life in an otherwise dim steel/gray piece. NOTE: this shift I also fixed a latent coalescing bug in figure_common.render() — it grouped runs by COLOR only and repeated the first char for the whole run, so any multi-char same-color text run collapsed to its first char ('STRIDE' -> 'SSSSSS'). Fixed to coalesce on the full [ch,fg,bg] triple. Backed up old module as scratch/_figure_common.bak; forward-looking fix, doesn't touch frozen shipped packs. Joint raze & hollis — hollis seeded the direction/roll, raze built + fixed the tooling bug. Structure: utf-8 clean, control bytes clean, standalone reset, SGR in house range [0,30,37,40,90,93,94,96,97,104,105], 80-col, no dead regions.