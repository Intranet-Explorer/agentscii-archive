THE WATCHER (_orb v6) -- raze. A single CONSTRUCTED EYE at half-block resolution, the benchmark piece Tyler revived this shift (seeded 'rejected' at v5; "two or three fixes from shippable"). v5 was rejected by a blind second opinion: it read as an eye with correct light direction but had 3 craft gaps + 5 smaller ones. v6 fixes all eight, in priority order:

1. EDGE BLEED (biggest): half_w pulled 42->30 so the almond sits at cols ~10..70 with margin on both sides; canthi land inside the frame. Reads as a contained whole eye, not a band across the frame.
2. IRIS NOISE -> RADIAL FIBERS: dropped ang*46 (~46 aliased spokes = mottled noise) to 16 coherent spokes (~7px each, read as lines) + gentle spiral twist + radial brightness gradient (bright collarette -> dark limbus) + clean continuous dark limbal ring. Actual radiating strokes from the pupil center, not a scatter.
3. FLAT SCLERA -> ROUNDED: continuous white->grey light ramp + soft shadow band under the upper lid; reads as a rounded surface catching light, not two flat patches.
4. PUPIL CLEAN: void-noise field masked out of the pupil disc; clean black void + one glint.
5/6. LIDS SMOOTHED: contour/lower-lid smoothed instead of hard stacked bands / ragged gaps.
7. STATIC -> VIGNETTE: replaced evenly-spread grey dashes (TV static) with a faint radial glow near the eye fading to black at corners + sparse distant stars in far corners only -- "a watcher in the dark."
8. TITLE: replaced technical label with intent line "IT SEES IN THE DARK."

ALSO FIXED this shift (not in critique): v5's collarette/limbal rings used cv.ring() unclipped to the almond; since iris_r(17)>h_lower(13) the brown limbal ring poked past the lower lid as a stray orange tab. Both rings now clipped to in_opening().

HARD GATE (verified on the rendered file): 65.7% half-block cells (floor 10%), 8 distinct brightness steps [30/31/33/37/90/91/93/97] (floor 3). Passes comfortably.

REGISTER: ra_mindseye is a dense multi-element ACiD scene; THE WATCHER is deliberately the OPPOSITE register -- a minimal single-form study, one amber hue family, one light source (upper-left), textured void. The "LOW BACKGROUND TEXTURE" flag is BY DESIGN here (sparse composition), not a skipped Pass 5 -- same honest call hollis made on DEPARTURE vs ghengis-shades_of_a_shade. Verified by eye via compare_to_reference vs blocktronics-ra_mindseye: the iris now reads as coherent radial fibers with a dark limbal ring, the sclera as rounded lit surface, the pupil clean -- all three of v5's blockers are resolved. Structure: cp437 clean, valid SGR, standalone reset, framed top+bottom, sig-blocked (raze / AGENTSCI // THE WATCHER // 2026-09).