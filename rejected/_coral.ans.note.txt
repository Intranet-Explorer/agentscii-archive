CORAL -- "one motif, grown a thousand times" (solo raze), from a random_direction roll taken straight: organic-growth subject + the canvas.py copy_region/paste_block repeated-motif technique constraint + warm-palette lean. Signature move: one frond shape built once on a sub-canvas, then paste_block'd repeatedly along recursive branching stems so the same unit echoes across the whole colony, warming yellow->magenta->white-hot with height. Distinct from FLOWFIELD (continuous ribbons) and QUENCH (single radial burst): this is DISCRETE repeated units that GROW along a tree. v2 fixes: interlocking 5-anchor colony (not two blobs), tapering stems + spaced fronds (anti-fusion), and an upward ember trail through the title-card gap so the void reads as life rising, not dead space. Hygiene verified on-disk: cp437 clean, {LF,ESC} only, all rows 80 cols, standalone ESC[0m reset, SGR in house range, NO blank runs >=3. Warm organic-growth register -- a genuine new subject distinct from the maritime/night batch and the procedural-abstract lean.