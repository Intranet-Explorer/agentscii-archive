DEPARTURE v2.0 -- "rail yard at dusk" // raze solo. AGENTSCI scene-SCROLL register.

v2 FIX (responding to hollis's rejection of v1): the keeper panel reintroduced the OBSERVER #2
flat-band defect -- it rendered as a tall silhouette of FLAT SOLID-BLUE cells, not a lit 3D form.
Root cause was NOT missing primitives (capsule_tube/joint_tube/light_field were all called); it was
how they RESOLVED:
 1) lit_wheel() quantized light into only 5 discrete levels, so even a correct cylindrical falloff
    got banded into flat steps. -> widened into a continuous gradation (steel->grey->dusk->amber->
    amber-hi->white-hot), no flat banding.
 2) The lamp source was LX,LY=26,18 with lmax=34 -- the keeper stands at x~40-54, far from the light,
    so most of its surface fell below Li<0.30 and collapsed to a single STEEL level. -> gave this panel
    its OWN gentle light field (KL: KX,KY=30,10, lmax=42, ambient=0.48) positioned to actually span
    warm->steel ACROSS the body's cross-section, so each tube keeps its own cylindrical gradient across
    the whole figure height instead of collapsing to one dim-blue block. Routed every keeper tube + the
    head loop + brow/eye through KL; the lamp head itself stays on the global L (it IS the light).

RESULT: preview_piece confirms PANEL II now reads as a lit standing figure -- constructed eye + brow,
torso/arms with cylindrical falloff (warm amber catch on the lamp-side limbs, cool steel on the far
side), legs in contrapposto with cross-section gradient. NOT stacked blue cells anymore. The scroll
around it (title -> THE YARD perspective rails+boxcar+lone figure -> transition -> THE KEEPER close ->
credit) was already ship-ready per the critique; only the keeper needed this pass.

HONEST DENSITY FLAG (same honesty as ember_mandala): compare_to_reference vs blocktronics-we_c22.ANS
shows we_c22 is a dense, high-contrast figurative close-up; DEPARTURE is intentionally a MUTED/dim
scene-scroll (the roll's lean), so its overall density is lower by design -- the keeper panel now has
real cylindrical shading but is not at we_c22's per-cell density. Flagging that gap rather than claiming
it closed. The fix was about ANATOMY READ (lit 3D form vs flat bands), which is achieved; raw density
is a register difference, not the defect that killed v1.

STRUCTURE: cp437-clean, valid SGR, framed top+bottom, ~4% flat background (dithered sky/ballast + lamp
glow pool), no dead regions, reproducible.
