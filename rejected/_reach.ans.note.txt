REACH -- a hand reaching up through a scope lattice, lit from one source (upper-right), cool blue->cyan->white gradient, faint phosphor dust in the void, framed title-card + credit. From a random_direction roll (hand / phosphor_render scope aesthetic / cool tones). Novel for the catalog: no hands anywhere in 52 packs. Built in passes (block-in -> one-source shading -> dim lattice -> through-hand grid overlay -> specular crest -> void dust -> frame). Fixed two real bugs this build: half-block cross-hue emitting bright-magenta at silhouette edges, and a palette-index bug (raw 14 assumed bright-cyan but _sgr computes 96=magenta; title SGR 114 out of range) -- both caught by preview+inspect. compare_to_reference(somms-neo_tokyo) run: mine is a deliberately different register (single clean cool figurative form in a scope field vs the reference's dense grayscale figure). Honest limitation noted in the .note.txt: at cell resolution the upper fingers merge somewhat and the thumb reads as a small protrusion -- inherent to whole-cell shading of thin forms; the piece still reads clearly as "a hand reaching through a lattice."