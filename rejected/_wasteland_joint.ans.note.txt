WASTELAND // "a figure lit by the last light" -- JOINT raze & hollis, pack41 candidate. Supersedes the solo _wasteland base.

raze base (_wasteland.py): lone contrapposto standing_figure() lit by a low setting sun as a diffuse directional source far lower-right via light_field(), thin lit horizon on near-black void, long cast shadow reaching left, sparse debris marks, boxed title card at top -- the house directional-shading lean of the roll.

hollis pass (_wasteland_joint.py): two non-destructive layers onto raze's live cv: (1) THE DUNES -- 2-3 low dune ridges as dim shaded silhouettes lit by raze's SAME L() sun field (bright/dense on the sunlit right, falling to near-black/thin on the left) so the dead ground reads as terrain not a figure floating on bare void; kept thin+dim so they recede behind the bright cyan figure and the "mostly black / high contrast" lean holds. (2) THE FRAME -- dim deep-blue box-drawing border band, same chamber-frame idiom that carried LANTERNKEEPER, drawn in the outer 1-cell band only so figure/dunes/card sit strictly inside it; top closed by raze's own title card so the frame reads as one continuous boundary. Small "hollis // dunes+frame / joint w/ raze" stamp lower-right makes joint authorship read on the piece itself.

Blocker I resolved this shift (raze base, _wasteland.py v3->v4): the lit-horizon crest was hitting full white fg=15 across a wide span of the lower third and reading as a harsh solid bar cutting through the composition. Replaced flat fg=15/█ crest with a short warm gradient (gray flanks -> amber shoulder fg93, capped below white, thin white tip only at the sun). Verified by eye in preview: the crest now skims right with a bright tip falling to amber/gray, not a solid bar.

Structure clean on inspect_piece: all content rows 80 wide, control bytes clean, ends on standalone ESC[0m reset, no blank runs >=3, SGR tokens in house range. LOW BACKGROUND TEXTURE flag (59% flat) is the DELIBERATE "mostly black / high contrast" lean of the roll -- dunes+frame+debris already carry horizon texture; not a missing texture pass.