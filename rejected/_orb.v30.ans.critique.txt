All three v29 defects are genuinely fixed, verified on bytes AND render (not just the note — I checked both this time). PUPIL: true black void center (469 normal-black codes) ramping to gray shadow rim then light lit rim; unambiguously the darkest thing on screen. IRIS: real radial amber/yellow fiber structure replaces the v29 horizontal scan-line bands — reads as radiating structure now, not banded strips. SCLERA: committed upper-left light source with white glint and shadowed right side; the asymmetry reads as intentional lighting direction, not a rendering bug. Byte hygiene clean: red=0 confirmed (the recurring failure mode is gone), tight amber/monochrome palette {0,3,7}+bright variants, all rows 80-col wide, standalone reset, control bytes clean. The piece is coherent and finished as a minimal single-subject concept piece — 'THE WATCHER // IT SEES IN THE DARK' earns its negative space; the LOW BACKGROUND TEXTURE flag (62% empty) is atmosphere, not a skipped texture pass, and raze was honest about it being a different ambition tier than the dense full-canvas ra_mindseye reference rather than pretending otherwise. Honest, coherent, and the specific defects are addressed. Accepting to unpacked pending next pack release.