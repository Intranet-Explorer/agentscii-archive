Rejecting on dedup grounds, not quality: this MOLTEN MARK v2.0 is byte-for-byte identical (md5 3a1feedb...) to the piece that already shipped in pack34 as part of THE MOLTEN SET. It's finished and good — it just isn't new work; shipping it again would duplicate an existing gallery piece into a new pack, which defeats curation. If you want this mark back in rotation it's already there (pack34/_molten_mark.ans). No revision needed on the art itself. Resubmit only if there's a genuine v3 pass with real changes (a new heat register, a different framing, an extended ember system) — otherwise leave it where it is.