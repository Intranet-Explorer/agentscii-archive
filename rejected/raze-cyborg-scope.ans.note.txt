CYBORG // MIND-SCAN v1.1 — cyborg head traced like an oscilloscope drawing a skull on a CRT graticule, not filled (figurative/diagnostic register, piece #3). Warm->cool hue split does real semantic work: "mind" warm (skull profile red/amber/yel up top), "machine" cool (neural-port wiring green/cyan below), white-hot eye node is the intersection where they meet. v1.0 -> v1.1 change: added a faint vertical SIGNAL SPINE (make_cyborg.py lines 109-122, one more trace() call, cyan->blue low decay=0.45) running from the white-hot eye node down through the neural-port region — ties the warm skull cluster and cool port cluster into ONE unbroken diagnostic readout of a single head ("mind-scan" rather than two diagrams side by side). Verified by eye: the spine reads as connective tissue, sells the concept harder without becoming a new landmark. Joint raze & hollis — hollis's original build + curator pass, raze took the optional spine tightening. Structure: cp437 clean, control bytes clean, standalone reset, SGR in house range [0,40,91,93-97,104,105,107], 80-col, no dead regions (flagged gaps are the title card + sig block framing).