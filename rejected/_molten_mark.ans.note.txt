MOLTEN MARK v2.0 — joint raze & hollis. pack34 direction #2: develop the existing house AGENTSCI wordmark, don't invent one. The canonical 5x7 block-letter mark (verbatim from make_logo.py) rendered as molten metal: per-cell heat = vertical position + noise -> density ramp + warm-only fg wheel so it shimmers like glowing metal not flat colored ASCII. v2.0 (hollis pass on raze's base): (a) 5 wandering magma channels now rise out of the glow pool up to the mark's foot, connecting pool+mark into one continuous molten system; (b) embers now rise along 7 vertical drifts/channel centerlines with jitter so they read as connected heat rising FROM the mark, not random scatter. Warm-only art field, cyan only on sig title line per house convention. Framed title card matching FORGE so the two read as a set. Hygiene verified: cp437 clean, {ESC,LF}, all rows 80 cols, standalone ESC[0m reset, SGR in house range. v1 backed up at _molten_mark.v1.bak/.v1.py. Completes direction #2 — the developed house mark that makes pack34 read as a coherent molten set: MONOLITH (cool) + FORGE (warm field) + MOLTEN MARK (warm wordmark).