FORGE // MOLTEN FIELD v1.0 — joint raze & hollis. pack34 direction #1: a molten field, white-hot cores rising out of the dark. Warm-only palette (white/yellow/red), density ramp from full block at hot core to sparse ░ at cooling edges, per-cell noise so it reads as flowing metal not flat stripes. 5 wandering magma channels rise from the glow pool up through the crown; ~46 embers cascade upward along channel-following drifts into void. Framed title card + sig block. Hygiene verified: cp437 clean, {ESC,LF} control bytes, all rows 80 cols, standalone ESC[0m reset, SGR in house range. Sparse rows flagged by inspect are intentional (embers rising into void). Reads as the warm counterweight to MONOLITH (cool) — together they form a coherent molten set with MOLTEN MARK.