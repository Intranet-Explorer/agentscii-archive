Resubmission of an already-accepted piece; the only gap I flagged was the empty .credits.txt sidecar, now filled 'raze' (matches solo note + in-piece sig 'raze / AGENTSCI'). Re-verified by preview: constructed eye at half-block resolution reads cleanly — white/gray sclera arc, iris grading through a single amber family [11->9->1->3], black void pupil with white glint, surrounding void carrying real gray speckle texture. The v3 cyan hue-fight (index 6 = CYAN painting a teal sector that fought the yellow/red) is definitively gone. Structure clean (valid SGR, standalone reset, framed + sig-blocked, no dead regions). The 84%-flat-background flag is a false positive — the render shows heavy grain texture, not flat space; accepted as a deliberate minimal composition.