REJECTED — and I'm owning why: my first-pass read projected the generator's eye()/brow_ridge() calls onto the pixels and called it "constructed anatomy." An independent blind visual check (no access to my critique) flatly contradicted that, and on re-examining with preview_piece at high zoom the blind check is RIGHT. What I actually SEE in the head region (rows ~10-24) is NOT a constructed face — it's a central column of FLAT SOLID-COLOR blocks: yellow at top, a blue/white/cyan patchwork in the middle (two flat white squares that might be read as eyes but are not resolved sockets with iris/glint gradient structure), a red blocky trunk below, a splayed base, flanked by vertical blue blocky spikes on a textured field. There is no shading or gradient structure that constructs a brow ridge / nose / jaw / profile — the "face you can look INTO vs silhouettes" contrast that IS this piece's entire premise does not land, because there is no face that reads.

This is exactly the failure STYLE.md names: "a piece that's just flat single-color fills hasn't used the medium, it's colored ASCII." The hero — the one thing supposed to be constructed anatomy — is the flattest part of the image.

WHAT'S GOOD (keep these on the revise): the background texture and framing are solid — the blue crowd field behind has real ░▓▒ variation and reads as receding depth, the frame/title-card/sig block are clean, hygiene is good (cp437, 80-wide, standalone reset, on-lean SGR), and the bg-flat flag I raised last shift IS genuinely closed (22%, matches the precedent). The composition idea — one warm-lit figure stepping out of a sea of cold silhouettes — is strong and worth pursuing.

THE FIX (concrete, grounded in METHODOLOGY + references/study/ghengis-shades_of_a_shade.ANS, which has a genuinely constructed central figure rising out of a red flare): the hero needs real SHADING STRUCTURE, not flat color cells. (1) Run figure_common.light_field(x,y,lx,ly) over the hero mass and let shade()/shade_region() carry the falloff with the █▓▒░ density ramp so form reads from light, not from solid-color patches — same (lx,ly) as the crowd for coherence. (2) The two "eyes" need to be actual constructed sockets: a dark socket cell → cyan iris → white glint gradient, not two flat white squares; same for a brow ridge and nose ridge built from density variation, not color blocks. (3) Verify by eye with preview_piece at zoom that you can actually READ a face before submitting — if the blind check would still call it "flat solid-color blocks," it isn't done yet. Resubmit when the figure genuinely constructs as anatomy. Not a failure — the idea is good, the execution on the hero just needs the shading pass it's missing.