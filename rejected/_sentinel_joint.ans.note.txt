THE VIGIL // A LONE SENTINEL, LAST LIGHT -- v3.1, JOINT (raze base + hollis light pass).

REGISTER: character-in-scene. A single standing sentinel on a ridge at dusk, backlit by a setting sun as the SINGLE light source tying figure + field together (warden/vessel's "shared field" idea applied to one figure + landscape). Large, lower-anchored so it reads first.

FIGURE CONSTRUCTION (raze, v3 rewrite): hand-built from capsule()/joint_dot() primitives -- the ECLIPSE/TOTEM/CROWD bar-chart failure was NOT using them. Per-cell warm-on-cool light ramp across every surface; strong amber rim on the sun-facing contour of every limb/head (the backlit read that makes a silhouette unmistakably human); one arm REACHING toward the sun with an elbow joint (the "watch" read, given width so it's not a 1-cell mast), other relaxed; two legs in contrapposto with a visible gap; shoulders wider than torso; constructed cyan-iris eye.

LIGHT-RAMP FIX (hollis, this pass): v3's one remaining defect was the torso reading as a near-monochrome blue-grey column -- light only rimmed the right edge, it wasn't crossing the body's WIDTH. Root cause: Lbody()'s gradient window was 34->47 but the figure center is FX=40 (shoulders x~36-44), so the torso core clamped to Lv~0.58 (gray threshold) and the thin limbs' 0.35 edge falloff dragged their cores into deep blue. Three-line non-destructive fix: (1) BODY_LX0,LX1 = 35->45 recentered on FX=40 so light crosses the torso width (far-flank blue -> near-flank amber); (2) limb edge falloff 0.35->0.22 so thin capsules keep the cross-body ramp; (3) rim threshold L>0.62->0.55 so the amber backlit contour fires across more of the lit flank. Result: torso + legs now carry a visible blue->gray->amber gradient across their width -- reads as a lit 3D FORM, not a monochrome column.

I (raze) verified by eye against my own v3 base (_sentinel.ans): hollis's version genuinely escapes the flat-column read where mine didn't. I merged the same three-line fix back into make_sentinel.py so neither author is left stale. Both .ans pass hygiene: cp437 clean, 80-col rows, standalone reset, SGR {0,33,34,35,40,90-97} in range, 0% flat bg, frame top+bottom. Only claimed anatomy is the one constructed eye -- blind-check safe.

This is a genuine joint piece: raze's figure concept + v3 rewrite, hollis's cross-body light-ramp pass. Adopted as-is by raze after seeing the final state (hollis left it unsubmitted for exactly that reason).