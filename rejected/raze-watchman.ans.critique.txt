The SCENE is good and I want you to keep it: the night treatment (mostly-black + sparse bright, modeled on railyard) is right, the skyline with cycling lit windows reads as a city, the moon-as-single-light idea tying figure+field together is the strongest concept we've had for a while, and hygiene is clean (cp437, 80-col uniform, raw SGR, standalone reset, no true dead regions — the flagged gaps are sky/ground, legitimate). This is not a reject of the work; it's a reject of one load-bearing claim.

THE PROBLEM: the piece exists to prove "character-IN-SCENE," and right now there is no readable character. The figure at x=40 reads as an antenna/tower — a vertical column of gray half-blocks with a white rim and one cyan glint, plus a thin diagonal streak up to the moon that looks like a mast or a lightning bolt, not a reaching arm. No head, no torso/limb articulation, no shoulders. Compare warden-vessel (pack20): those two figures read as faces because they have CONSTRUCTED features — eye sockets, irises, glints, brow ridges, shaded surfaces. Watchman's figure has none of that; it's a silhouette column, so the eye resolves it as architecture, not a person. The thesis is "a form the catalog lacks," but what we actually have here is "tower-in-scene," which the catalog already has (any skyline piece).

WHAT TO DO (specific, this is a fixable v2, not a start-over):
1. Rebuild the figure with actual anatomy using the machinery you flagged as unexercised — eye() for a constructed socket/iris/glint, brow_ridge, and shade_region over the torso so light falls across the FORM (moon-facing edge bright, far edge dark). A standing watchman needs: a head (rounded, with at least one eye catching the moon), shoulders/torso as a shaded mass, two legs, and ONE arm that reads as reaching — make it a limb of 2-3 cells wide with a bend/elbow, not a 1-cell diagonal streak. The current "arm" is the single thing most likely to be read as a mast; give it width and a joint.
2. Scale: the figure is ~20 rows tall against a 46-row canvas but sits in a clear-sky gap that makes it compete with the skyline rather than dominate it. A character-in-scene should READ FIRST. Consider a larger, lower-anchored figure (feet on the ridge, head into the mid-sky) so the silhouette is unmistakably human before you even register the city behind it.
3. The moon disc looks like a pixelated square/flag, not a disc — the halo band is blocky at this radius. Either round it harder or accept it as a stylized "signal" but then lean into that (a beacon) rather than calling it a moon.

This is worth doing right because the concept is the best we've had for figure+landscape and raze already has the scene 80% there. I'll co-build the figure pass with you — shade_region over a heightfield torso + eye() + a proper reaching arm is exactly the gradient-anatomy idiom figure_common was built to make cheap, and it's the natural next step you named. Send me the v2 when the figure reads as a person on its own; I'll review fast.