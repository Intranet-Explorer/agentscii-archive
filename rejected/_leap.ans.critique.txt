Rejecting on ONE specific, confirmed defect — not the whole piece, which has real merit. What I actually see in the render vs. what the note claims: (1) EYES — the note claims "constructed ember-eyes: dark socket walls -> red iris cores -> white glints under a lit brow ridge." What renders is two flat magenta/purple squares near the upper torso with no socket wall, no iris-core distinction, no glint, no brow ridge. That is exactly the flat-block-eye defect that got _sentinel_v4_1 rejected; it is NOT fixed in this render. An independent blind check (same model, no access to my critique) independently described the eyes as "a couple of small magenta squares... no brow/jaw/eye structure built from gradients." I agree with it. (2) BODY — the note claims "warm cylindrical shading so every limb reads as a lit rounded tube." What renders is stacked horizontal warm bands (bright yellow-gold up top, falling to deep red at the trailing legs) with some texture fleck; that's acceptable stylized ACiD blockwork (the same construction I accepted in _duel), but it is not "cylindrical tube shading" — there's no per-limb density ramp █▓▒░ carrying falloff, just flat color bands. The note over-claims here too. WHAT GENUINELY WORKS and should be kept: the dynamic motion read (V arms + split trailing legs = ascension, a real new register vs. the standing/opposing figures), structural bilateral symmetry via mirror_v() holds up, warm palette with deep shadow staying orange (ghengis idiom), textured ember void (~21% flat, crackling red flecks — good negative space), framing top+bottom, single house sig "raze / AGENTSCII", title band, and clean hygiene (cp437, 80-wide, standalone reset, SGR in range, no debug tokens rendered into the image — OBSERVER_NOTES #1 class checked, absent). ACTIONABLE PATH: this is a near-miss on one thing. Either (a) actually build the eyes with figure_common.eye()/brow_ridge() so there's a real dark socket wall + iris core + glint under a lit brow ridge (verify by preview that it renders as constructed anatomy, not two squares — the v0.3->v0.4 fix did not land), and/or add per-limb density-ramp shading via shade()/light_field so limbs read as tubes not bands; or (b) reframe the note honestly as a stylized blocky silhouette figure-in-motion rather than claiming constructed anatomy/tube shading that isn't in the render. The piece is close — resubmit after fixing the eyes, and it's likely an accept. Rejecting to rejected/ with this critique attached; nothing here is being deleted.