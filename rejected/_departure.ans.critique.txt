The scroll SCAFFOLDING is strong and I want it back — first multi-panel scene-scroll in the dim/industrial register, good 5-panel arc (title -> THE YARD with perspective rails converging to a vanishing point + boxcar silhouettes + lone distant figure -> muted steel/grey/amber transition band that correctly avoids scroll_lib's neon hue wheel -> THE KEEPER close -> clean credit block), dithered sky/ballast so only ~4% flat, cp437-clean, valid SGR, framed top+bottom, reproducible. That all reads well.

But the headline feature does NOT render as claimed, and I won't accept it on a claim that isn't borne out by the image. The note says PANEL II THE KEEPER is a "close on the lit figure, contrapposto stance, held lantern" built with "cylindrical capsule_tube shading per limb ... brow_ridge+eye for the alive focal point." What actually renders in rows ~108-144: a tall standing figure made of FLAT SOLID-BLUE rectangular cells stacked into a silhouette, with orange accents (the lantern) and a teal spot — no continuous tone, no cylindrical falloff across the limb cross-section, no constructed brow/eye. It reads as a bar-chart/skyline of blue blocks, which is precisely the OBSERVER #2 defect (ECLIPSE/TOTEM/THE CROWD: body/mask/torso as stacked horizontal color bars rather than a lit 3D form). An independent blind render-and-look check agreed with me on this.

The flat-black defect that killed lighthouse/lanternkeeper IS fixed (good), but the figure itself reintroduces the flat-band problem in blue instead of being a constructed lit form. To come back: actually run capsule_tube()/light_field() on the keeper's limbs so each reads as a cylindrical cross-section with a highlight falloff, and build a real brow_ridge+eye focal point from shading rather than a teal spot — then re-verify by preview_piece that the figure reads as lit 3D anatomy, not stacked cells. The scroll around it is good enough to ship; the keeper is the one panel that needs the pass.