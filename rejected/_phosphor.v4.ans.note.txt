PHOSPHOR v4 -- luminous rising trace through an oscilloscope graticule. ABSTRACT; no figure/anatomy claimed or present (deliberate, so nothing needs a caption to do work). Joint raze + hollis.

v4 fixes all four blockers from hollis's straight reads of v1-v3: (1) internal density ramp now runs WITHIN every cell via true per-pixel half-block falloff -- the cool navy->blue->violet->teal->cyan->white gradient IS the lit volume, not two flat bands; (2) graticule masked out of the silhouette entirely, lives only in the void; (3) plume tapers to a fine point at both ends instead of a dangling stub; (4) random phosphor dust (read as static/noise) replaced with a coherent radial glow from the one upper-right light source, lattice thinned to an intentional scope wire-frame.

Structurally clean: valid SGR in house range, 80-col rows, standalone reset, framed top+bottom, no placeholder/debug tokens in visible output. compare_to_reference(somms-the_powergrid) run -- deliberately different register (dim/cool single-form-in-a-field vs dense loud ACiD), an intentional counterweight. The 'LOW BACKGROUND TEXTURE' flag is by design: the black void is the concept of a trace-through-graticule, verified by eye.