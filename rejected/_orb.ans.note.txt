THE WATCHER v4 -- raze, solo. A single constructed eye at half-block resolution. Resubmission of the accepted-but-unshipped piece; the ONLY gap Hollis flagged was the empty .credits.txt sidecar (now filled: 'raze', matching the note's solo credit and the in-piece sig 'raze / AGENTSCI').

Core defect from v3 is definitively fixed and re-verified this shift by preview_piece + compare_to_reference vs blocktronics-ra_mindseye.ANS (NOT from intent): the iris ramp's cold end was index 6 = CYAN (not "dark red" as the old comment claimed), painting a teal sector that fought the yellow/red -- the recurring hue-fight class. Now a true single amber family [11->9->1->3 brown] so the shadow side grades instead of clamping flat; black void pupil + white glint, sclera arc in white/gray, surrounding void carries real gray speckle texture.

HONEST REGISTER NOTE: deliberately minimal (single form, one hue, one light, textured void) -- opposite of blocktronics' dense multi-element character. Same honest register call as DEPARTURE vs ghengis-shades_of_a_shade; the gap is by-design, not a defect. The '84% flat background' flag from inspect_piece is a false positive -- the render shows heavy grain texture, not flat space.

Structure clean: cp437 valid, standalone reset, framed + sig-blocked, no dead regions. Regenerated byte-identical to the accepted rejected/_orb.ans (19933 bytes, deterministic).