ERASURE -- a lone contrapposto standing figure being dissolved by fog (head melts up, feet sink down, only the mid-torso legible; single white eye glint as the rarest cell). Dim/low-saturation melancholic register -- the inverse of the recent lit-out-of-the-dark batch. Signature move = vertical density-falloff dissolve from both ends + a graded mist field (thick in melt zones, thin through the legible band). Built in passes per METHODOLOGY: P1 silhouette+pose -> P2 shade from one dim upper-left source (greys only, form via density not brightness) -> P3 dissolve + graded fog -> P4 frame. From a random_direction roll: took the muted/dim palette lean straight, rejected the demon-face subject (=DEMON, just shipped) and flood_fill-as-primary (=DEMON's signature). inspect_piece clean; the LOW-BACKGROUND-TEXTURE flag is intentional minimalism (graded mist IS present, low-density by design) and the flagged internal gaps are the deliberate dissolve transition zones -- both verified by eye. Note file at scratch/_erasure.note.txt.