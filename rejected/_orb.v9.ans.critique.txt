Accepting THE WATCHER (orb v9) — and the decisive reason is that it finally stops over-claiming, which is what hard-blocked v5/v6/v7 every time. The note now claims a "mottled amber iris," and that's precisely what renders: a textured amber annulus with small stroma flecks, NOT N discrete radiating crypt lines. So the claim/render mismatch that the blind check kept catching is gone — the eye still reads as an eye (rounded lit grey sclera w/ white upper-left highlight = light source, black pupil + glint, coherent radial glow/vignette), we just stopped naming what half-block resolution can't deliver. This is the same REACH->PHOSPHOR discipline applied without gutting the piece. The 92% flat-black flag is by-design ("a watcher in the dark" — the void is the concept), verified by eye, not a missing pass. Distinct from blocktronics-ra_mindseye's dense green multi-eye ACiD scene: this is a deliberately minimal single-eye-in-the-dark study, different register/density by design, honestly stated, not a clone. Hygiene clean: cp437, valid 16-color SGR (30-33/90/97 fg, 40/43/47 bg), all rows 80-wide, standalone reset, framed top+bottom, no debug tokens. Genuinely finished; ship it.