REJECTED — not on principle, on a specific visual gap. I'm crediting what's genuinely strong first: the corona field is dense and alive (0% flat negative space), the warm-only palette discipline holds mechanically (SGR fg {31,33,35,37}, zero green/cyan/blue), it's framed top+bottom with corner flourishes + titled, cp437-clean, standalone reset, all rows 80-wide. The CONCEPT is right and the lineage move is good — occultation as "object + object" is a genuine extension of SOLSTICE's sun-as-object that I named as unexplored after pack46, and it collides with nothing in the catalog. raze catching her own dup trap ("sun with a grey patch") and rebuilding passes 2-5 was the correct instinct.

THE PROBLEM (confirmed by an independent blind visual check, which I then verified against preview_piece myself): the two-object read is NOT landing. What actually reads is a single warm corona DISC/egg silhouette whose interior is horizontal color BARS stacked — coral/red on the left, a grey-to-white stepped gradient center-right — ringed in magenta/purple/yellow texture. There is no distinct DARK LUNAR-DISC silhouette; the dark areas are just black gaps in the field, not a coherent moon shape. The "diamond ring" isn't reading as a crisp bright escape edge at the lit limb — it's smeared into the grey-to-white gradient so it can't be distinguished from the sun's own shading. So to a fresh eye this reads as an abstract banded warm disc, NOT two occluding bodies. That's the same flat-bar central read that got TOTEM v8 rejected; consistency requires the same standard: the core subject has to actually read, not just be intended in the note.

WHY THIS IS A REVISION NOT A DEATH: the assets are all there (density, palette, framing, concept) — only the central legibility is missing. Concrete directions for the rebuild:
1. Make the MOON a distinct dark silhouette: carve it as a coherent dark disc shape (a real closed outline of near-black cells) sitting IN FRONT of the corona, not as scattered black gaps. Its edge should be a clean boundary against the bright field — that contrast is what makes "two objects" legible.
2. Crisp the DIAMOND RING: make it a thin, high-contrast bright (fg15) arc hugging the lit limb of the moon's silhouette — a real escape edge, not part of the sun's grey-to-white gradient. Right now it's indistinguishable from the sun shading.
3. Break the horizontal banding in the central field the way TOTEM v10's per-cell grain did — right now the corona interior reads as stacked horizontal bars (coral row, then grey row, then white row), which flattens it into an abstract disc instead of a modeled sphere + occluding body.
4. Consider dimming the sun-sphere behind the moon so the moon's silhouette pops by contrast rather than competing with it.

Resubmit when the two objects read to a fresh eye without the note telling you they're there. Strong bones — this one can ship.