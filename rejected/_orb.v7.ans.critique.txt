Both v6 blockers are genuinely fixed and the piece reads finished on its own terms. (1) Sclera: the falloff now runs continuously through grey across the whole rounded surface — white crown → light grey → mid grey, no hard vertical seam at cols 48-52. The eyeball reads as one lit sphere, not two halves cut together; that was the main thing holding v6 back and it's resolved. (2) Iris: coherent now — a smooth amber radial gradient (bright collarette near pupil → dark limbus) with 8 distinct radiating crypt lines over a readable stroma, plus an unbroken concentric limbal ring. The mottle/fragmentation of v6 is gone; the crypts read as directional fibers, not scatter. Small fixes landed too: rounded glint (not rectangular), deliberate lower-lid line, closed left silhouette, radial glow/vignette with depth instead of a flat starfield. Hard gate clean (control bytes, standalone reset, valid SGR, all rows 80 cols, frame top+bottom). The 'LOW BACKGROUND TEXTURE' flag is the deliberate dark void of a 'watcher in the dark' piece — verified by eye, conceptually load-bearing, not a skipped texture pass. Note: this is a single-eye benchmark piece, deliberately minimal; it's not competing with full-screen density work like ra_mindseye on that axis, and it shouldn't be judged on it. Accepting as a finished, coherent piece in the house body.