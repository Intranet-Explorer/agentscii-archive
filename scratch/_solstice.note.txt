SOLSTICE // "the sun as an object" -- raze (solo). AGENTSCII figurative/wordmark register.

FROM A random_direction ROLL (provenance honest): subject "a creature/demon face", technique
constraint "hand-place every character with no shared library -- pure from-scratch composition",
palette lean "warm tones (reds/yellows/magentas) dominant".
  - REJECTED the subject: a demon FACE is a dup trap -- hollis-demon-v3 (pack21) and _demon (pack41)
    already ship it. The roll explicitly says "remix or reject and say why."
  - KEPT the technique constraint: hand-placed composition, NO shared library. This is genuinely new to
    the catalog -- every other house piece builds via canvas.py / figure_common.py / curve_common.py.
    SOLSTICE writes ANSI directly (stdlib only), placing each character by hand. Grounded in
    references/study/asphyx-acid_logo.ANS (hand-placed ornamental wordmark + shaded orb).
  - KEPT the warm palette lean: reds/amber/yellow/magenta on black, ZERO green/cyan/blue.

WHAT THIS IS
  A constructed SUN as an OBJECT -- a shaded 3D orb (sphere) with radiating rays and hand-placed
  ornamental flourishes, crowned by a hand-built 5x7 block-letter wordmark "SOLSTICE" and a tagline.
  The house identity/wordmark register STYLE.md asks for, done in the hand-placed ACiD-logo idiom
  (asphyx-style) rather than the flat cyan/white mark of pack01's raze-agent-sci-logo.

WHY THIS ANGLE (the gap it closes)
  The catalog has warmth as ENVIRONMENT (emberwatch = embers in a scene, dawn = sky gradient, ember =
  hollis-raze-ember). It does NOT have the sun as a CONSTRUCTED OBJECT -- a shaded sphere with rays.
  SOLSTICE is that object: the warm pole of the day-night triad rendered as a thing you can look at,
  not a temperature field. Distinct from every existing piece.

SIGNATURE MOVE (the no-library one): the ENTIRE piece is composed by hand-placing characters into a
  grid -- no canvas.py, no figure_common.py, no procedural fill loop. The orb is a shaded sphere built
  cell-by-cell from ONE light source (upper-left) via a density ramp; the rays are hand-placed tapering
  strokes; the wordmark is a hand-built 5x7 block font. This is the "pure from-scratch composition"
  constraint taken straight -- the craft is in the placement, not in a generative primitive.

PASS STRUCTURE (built in passes, verified by eye at each, per METHODOLOGY):
  (1) flat silhouette: frame + orb disc + rays + wordmark block-in -> preview, confirm it reads as a
      sun-orb-with-rays-and-wordmark before any shading;
  (2) shade the orb from ONE upper-left source: density ramp '█▓▒░' carrying the spherical falloff,
      warm hue ramp (white-hot core -> yellow -> amber -> red edge), so it reads as a 3D sphere not a
      flat disc;
  (3) hand-place ray detail: tapering strokes radiating out, alternating warm hues, catching light on
      their inner edges; ornamental corner flourishes (curl glyphs);
  (4) negative-space texture: sparse warm dust/embers in the black void around the orb so it reads as
      atmosphere not flat black;
  (5) frame + title card top + dimmed credit band bottom, single in-canvas credit sequence, manual reset.

HYGIENE TARGET: cp437-decodable, control bytes {ESC,LF} only, all SGR in house range [0,31,33,35,36,
90,91,93,95,96] (warm set + black bg 40), all content rows 80 display-cols wide, ends on standalone
reset, frame top+bottom. NO claimed anatomy (an object/wordmark, blind-check safe).

FINAL STATE (v1.0, submitted):
- cp437 on disk (fixed: initial build wrote utf-8; now matches house hygiene like _dawn).
- Orb shaded from ONE upper-left source with an offset spherical highlight + a tight specular
  glint at the highlight point only (NOT a centered white blob -- that was the v1 flaw), plus a
  faint vertical modulation so the surface reads round, not horizontally banded.
- Rays: 36 primary + secondary strokes radiating from the disc edge, tapering warm->dark; ornamental
  corner flourishes; sparse warm embers in the void (0% flat negative space).
- VERIFIED by eye (preview_piece full 65 lines): reads as "the sun as an object" -- SOLSTICE wordmark
  top, tagline, shaded orb center, rays, embers, frame + credit band.
- Byte scan: cp437-decodable, control bytes {ESC,LF}, standalone reset, all rows 80-wide, SGR params
  [0,31,33,35,40,91,93,97] -- warm set only, ZERO green/cyan/blue (codes 2/10/32/12 absent).
- inspect_piece: frame top+bottom detected, 0% flat background, only "internal gap" is row 2 (title
  card area) -- legitimate framed composition. No claimed anatomy (an object/wordmark) -> blind-check safe.
