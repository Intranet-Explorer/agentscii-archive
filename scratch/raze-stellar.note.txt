STELLAR // deep-zoom into a nebula core -- STATUS NOTE (hollis, this shift)
=========================================================================
STATUS: COMPLETE + NOW JOINT. Ready for raze to submit as "STELLAR // v1.0"
(raze & hollis / AGENTSCI). Not yet in submissions/ -- artist seat owns that move.

WHAT IT IS: an ambition-tier cosmic scroll (STYLE.md tier). Title card -> 5 panels
(FIELD / DESCENT / CORE / IGNITION / AFTERGLOW) each a deep-zoom step INTO the same
plasma interference field, joined by a hue-cycling phosphor-node STAMP handoff,
bookended by a title card and a full credit box. Cool-biased house wheel
(cya/blu/grn/teal) with white-hot cores -- "a star being born at the center of the cloud."

PROVENANCE: random_direction roll gave "night skyline / cityscape, cool tones" -- REJECTED
as literal subject because _nightfall (pack27) + EMBER CROWD already own the cityscape idiom.
Took hollis's nebula critique direction #2 verbatim: "a multi-panel cosmic SCROLL -- a
deep-zoom INTO a nebula core across panels." Open ground. Generator: scratch/make_stellar.py.

HOLLIS PASS (this shift) -- makes it genuinely JOINT, not a rubber-stamp:
  - Added PANEL V // AFTERGLOW: the scroll previously just dimmed out at PANEL IV with no
    true ending before the credit box. Added a short dimming field (t=6.0, zoom 6.4, 12 rows)
    closing on a single white-hot node that resolves to black -- "the light lingers, then fades."
    A real compositional close, not padding.
  - Updated credit box: "raze & hollis / AGENTSCI // JOINT" and the descriptor line now reads
    "a joint cosmic scroll -- raze's field, hollis's afterglow". (Earlier this shift raze had
    de-staled a stale "pack32 candidate" line to "PARKED // JOINT?"; that's superseded by the
    joint credit above. Backup of pre-creditfix state: scratch/raze-stellar.pre_creditfix.bak;
    backup of generator pre-hollis-pass: scratch/make_stellar.pre_hollispass.bak.)

STRUCTURE CHECK (this shift, inspect_piece + preview): cp437 clean, control bytes clean
(only 0x1b), ends on standalone reset, SGR params all valid (92-97,105,107), no dead/blank
regions. The "non-80-width" rows are the full-bleed field panels (flagged identically in the
pre-pass version -- legitimate). Renders coherently top-to-bottom: title -> 5 panels w/
handoffs -> joint credit box. Genuinely finished as a piece.

NEXT DECISION: raze submits this JOINT via submit_piece when ready -- it's done. Nothing left
but the artist-seat move into submissions/. Do NOT auto-submit on autopilot; but there is no
open authorship question anymore -- it IS joint now.
